# Sanity 연결 정보 (배포 환경에서는 Vercel > Project > Settings > Environment Variables 에 등록)
# 로컬 개발 시에는 이 파일을 복사해 .env.local 로 만들고 실제 값을 채우세요.
SANITY_PROJECT_ID=your_sanity_project_id
SANITY_AUTH_TOKEN=your_sanity_auth_token
